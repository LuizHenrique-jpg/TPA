package tpaProjectApostila;
//import java.util.Scanner;
public class Exercicio4{
public static void main(String[] args){
	/*Scanner scanner = new Scanner(System.in);
    
	System.out.println("Digite os valores de A, B e C:");
    
	double a = scanner.nextDouble();
    double b = scanner.nextDouble();
    double c = scanner.nextDouble();
    double delta = b * b - 4 * a * c;

    if (delta < 0) {
        System.out.println("Não existem raízes reais para a equação.");
    } else if (delta == 0) {
        double raiz = -b / (2 * a);
        System.out.println("Existe apenas uma raiz real para a equação: " + raiz);
    } else {
        double raiz1 = (-b + Math.sqrt(delta)) / (2 * a);
        double raiz2 = (-b - Math.sqrt(delta)) / (2 * a);
        System.out.println("Existem duas raízes reais para a equação: " + raiz1 + " e " + raiz2);
    }
    scanner.close();*/
}}
